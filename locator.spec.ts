// import { test, expect, } from "@playwright/test";

// test("verify playwright locator", async ({page})=>{
// await page.goto("https://demo.nopcommerce.com/");


   //const logo:Locator=  page.getByAltText("nopCommerce demo store");


//await expect(logo) .toBeVisible();


//await expect(page.getByText("Welcome to our store")) .toBeVisible(); // providing substring also allowed
//  await expect(page.getByText("wl")).toBeVisible();

//  await page.getByRole("link",{name:'Register'}).click();

//  await expect(page.getByRole("heading", {name:'Register'})).toBeVisible();

//  await page.getByRole("link" , { name:'Register'}).click();
// await expect (page.getByRole("heading", {name:'Welcome to our store'})).toBeVisible();

  

// });






// import { test, expect } from "@playwright/test";

// test("verify playwright locator", async ({ page }) => {
//   await page.goto("https://demo.nopcommerce.com/");

//    await expect(
//     page.getByRole("heading", { name: "Welcome to our store" })
//   ).toBeVisible();

//  await page.getByRole("link", {name:'Register'}).click();


//GetByLevel

// await page.getByLabel("FirstName").fill("robiul");
// await page.getByLabel("LastName").fill("awal");
// await page.getByLabel("Email").fill('abc@gmail.com');



// await page.getByPlaceholder("Search store").fill('Apple MacBook Pro');


// });
